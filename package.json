{
  "name": "opencode-tree-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "for d in packages/*/; do (cd \"$d\" && bun run build) || exit 1; done",
    "typecheck": "for d in packages/*/; do (cd \"$d\" && bun run typecheck) || exit 1; done",
    "test": "bun test"
  },
  "devDependencies": {
    "@opentui/core": "^0.5.10",
    "@opentui/keymap": "^0.5.10",
    "@opentui/solid": "^0.5.10",
    "@types/bun": "latest",
    "solid-js": "^1.9.12",
    "typescript": "^5.8.2"
  }
}
